# Mapbox public access token (required for map, satellite tiles, elevation API)
# Get yours free at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=pk.ey_YOUR_TOKEN_HERE

# Backend API base URL (stubbed with mock data during development)
VITE_API_BASE_URL=http://localhost:8000
